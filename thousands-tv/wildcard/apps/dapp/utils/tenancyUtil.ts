import { DEFAULT_THOUSANDS_PRIMARY_LOGO_URL } from "@/constants";
import { UserServerPreferences } from "@/types";

export const DEFAULT_SERVER_CODE = "thousands";

export const DEFAULT_USER_SERVER_PREFERENCES: UserServerPreferences = {
    serverPrimaryLogoUrl: DEFAULT_THOUSANDS_PRIMARY_LOGO_URL,
    serverCode: DEFAULT_SERVER_CODE,
};
